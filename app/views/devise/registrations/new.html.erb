<div class="row">
    <div class="col-md-6 col-md-offset-3 text-center ">
      <% if params[:plan] == '2' %>
    
        
        
    
    
     <h1> pro</h1>
     <p>10€ par mois bla bla blablalvbal</p>
    
      <% else %>
     
      <h2>basic</h2>
           <p>bla bla blablalvbal</p>
      <% end %>
    </div>
  
 <div class="col-md-6 col-md-offset-3">
    <div class="well">
        <h2>Sign up</h2>
         <% if params[:plan] == '2' %>
             <%= render"pro_form" %>
        <% else %>
            <%= render"basic_form" %>
        <% end %>

          
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>